debug: true

server:
  port: 8383

spring:
  application:
    admin:
      enabled: false
  # ===============================
  # = DATA SOURCE
  # ===============================
  datasource:
#    url: jdbc:mysql://localhost:3306/ukeess_test_db?createDatabaseIfNotExist=true&serverTimezone=UTC
#    username: root
#    password: secret

    ### TEST CONTAINERS
    url: jdbc:tc:mysql:8.1.0:///ukeess_test_db?TC_REUSABLE=true
    username: test
    password: test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    show-sql: true
    generate-ddl: true
    open-in-view: false
    hibernate:
      ddl-auto: create
  test:
    database:
      replace: none
  flyway:
    enabled: false

# ===============================
# = LOGGING
# ===============================
logging:
  level:
    org.springframework.test.context.cache: DEBUG
    org.hibernate: debug
    com.ukeess: debug
    sql: debug
    web: debug
